name: Verify DEV314 single activator

on:
  push:
    branches: [ceo-update-channel]
    paths:
      - '.github/workflows/verify-dev314-single-activator.yml'
      - 'ceo-updates/ACTIVAR_DEV314_NATIVE_TRANSPORT_INTEGRITY_UNICO.cmd'
      - 'ceo-updates/DEV314_NATIVE_TRANSPORT_INTEGRITY_ACTIVAR.ps1'

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ceo-update-channel
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Start mock CEO 1.5.86
        shell: pwsh
        run: |
          @'
          import json
          from http.server import ThreadingHTTPServer, BaseHTTPRequestHandler
          from pathlib import Path

          class H(BaseHTTPRequestHandler):
              def log_message(self,*a): pass
              def sendj(self,row,code=200):
                  b=json.dumps(row).encode()
                  self.send_response(code)
                  self.send_header('Content-Type','application/json')
                  self.send_header('Content-Length',str(len(b)))
                  self.end_headers()
                  self.wfile.write(b)
              def do_GET(self):
                  if self.path=='/api/health':
                      return self.sendj({'ok':True,'version':'1.5.86-rc1-provider-session-integrity'})
                  if self.path.startswith('/api/update/status'):
                      return self.sendj({
                          'current_version':'1.5.86-rc1-provider-session-integrity',
                          'available':True,
                          'remote':{'version':'1.5.90-rc1-native-transport-integrity'}
                      })
                  return self.sendj({'error':'not_found'},404)
              def do_POST(self):
                  n=int(self.headers.get('Content-Length') or 0)
                  body=self.rfile.read(n)
                  Path('dev314-request.json').write_bytes(body)
                  if self.path=='/api/internal-release/activate-prepared':
                      return self.sendj({
                          'ok':True,
                          'status':'PUBLISHED',
                          'version':'1.5.90-rc1-native-transport-integrity',
                          'remote_verified':True
                      })
                  return self.sendj({'error':'not_found'},404)

          ThreadingHTTPServer(('127.0.0.1',8775),H).serve_forever()
          '@ | Set-Content -Encoding UTF8 mock314.py
          Start-Process python -ArgumentList "mock314.py" -WindowStyle Hidden
          Start-Sleep -Seconds 2

      - name: Run DEV314 single activator
        shell: cmd
        env:
          CEO_ACTIVATOR_NO_PAUSE: "1"
        run: |
          call ceo-updates\ACTIVAR_DEV314_NATIVE_TRANSPORT_INTEGRITY_UNICO.cmd
          if errorlevel 1 exit /b %errorlevel%

      - name: Verify request and diagnostics
        shell: pwsh
        run: |
          $j=Get-Content dev314-request.json -Raw | ConvertFrom-Json
          $expected='https://raw.githubusercontent.com/danisuarezinef-ai/investment-intelligence-radar/ceo-update-channel/ceo-updates/DEV314_MANIFEST_UNSIGNED.json'
          if($j.manifest_url -ne $expected){throw 'wrong manifest url'}
          $log="$env:LOCALAPPDATA\CEO de IAs\diagnostics\DEV314_NATIVE_TRANSPORT_INTEGRITY_ACTIVACION.log"
          if(-not (Test-Path $log)){throw 'missing log'}
          $t=Get-Content $log -Raw
          if($t -notmatch 'DEV314 publicada y verificada'){throw 'success missing from log'}
          if($t -notmatch 'remote=1.5.90-rc1-native-transport-integrity'){throw 'remote visibility check missing'}
          if($t -notmatch 'CEO 1.5.86 localizado'){throw 'source version not detected'}
          Write-Host 'DEV314_SINGLE_ACTIVATOR_PASS'
