import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const EXPECTED_HASH="1e96928c7781f339732602234a1047fd510e38de04768d171884166ba5ae91e3";
const sb=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,{auth:{persistSession:false}});
const INSERT_ONLY_TABLES=["predictions","learning_cycles","model_evaluations","market_regimes","causal_edges","thesis_history","portfolio_recommendations","signal_weak_events","audit_events","historical_lab_runs","historical_fold_results","historical_challenger_results","brain_evidence","brain_transfer","brain_vault_events","brain_generation_runs","decision_research_ledger","decision_committee_votes","decision_negative_results","decision_audit_bundles"] as const;
async function sha(v:string){const d=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return [...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,"0")).join("");}
function response(s:number,p:unknown){return new Response(JSON.stringify(p),{status:s,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});}
function rows(body:Record<string,unknown>,table:string,node:string){const input=Array.isArray(body[table])?(body[table] as Record<string,unknown>[]).slice(0,3000):[];return input.map(row=>({...row,origin_node:row.origin_node||node}));}
async function insertOnly(table:string,input:Record<string,unknown>[]){if(!input.length)return 0;const {error}=await sb.from(table).upsert(input,{onConflict:table==="predictions"?"origin_node,origin_id,horizon":"origin_node,origin_id",ignoreDuplicates:true});if(error)throw new Error(`${table}: ${error.message}`);return input.length;}
async function converge(table:string,input:Record<string,unknown>[],key:string){if(!input.length)return 0;const {error}=await sb.from(table).upsert(input,{onConflict:key});if(error)throw new Error(`${table}: ${error.message}`);return input.length;}
function observedAt(r:Record<string,unknown>){return String(r.finished_at||r.completed_at||r.started_at||r.ts||r.updated_at||r.created_at||r.last_seen_at||new Date().toISOString());}
async function persistAutonomy(body:Record<string,unknown>,node:string){
 const singletonSpecs=[["state","radar_autonomy_state"],["paper_state","radar_paper_state"]] as const;const out:Record<string,unknown>={ok:true};
 for(const [key,table] of singletonSpecs){const value=(body[key]&&typeof body[key]==="object")?body[key] as Record<string,unknown>:null;if(value){const {error}=await sb.from(table).upsert({origin_node:node,updated_at:String(value.updated_at||value.last_rebalance||new Date().toISOString()),payload:value,real_trading:false},{onConflict:"origin_node"});if(error)throw error;}out[key]=value?1:0;}
 const specs=[["runs","radar_autonomy_runs"],["experiments","radar_autonomy_experiments"],["soak_ticks","radar_autonomy_soak_ticks"],["paper_positions","radar_paper_positions"],["paper_trades","radar_paper_trades"],["portfolio_values","radar_portfolio_values"],["experiment_memory","radar_experiment_memory"]] as const;
 for(const [key,table] of specs){const input=Array.isArray(body[key])?(body[key] as Record<string,unknown>[]).slice(0,3000):[];const packed=input.filter(r=>r&&r.id!=null).map(r=>({origin_node:node,origin_id:String(r.id),observed_at:observedAt(r),payload:r,real_trading:false}));if(packed.length){const {error}=await sb.from(table).upsert(packed,{onConflict:"origin_node,origin_id"});if(error)throw error;}out[key]=packed.length;}
 out.real_trading=false;return out;
}
async function exact(table:string,node:string){const {data,error}=await sb.from(table).select("payload").eq("origin_node",node).order("observed_at",{ascending:true}).limit(5000);if(error)throw error;return data||[];}
async function singleton(table:string,node:string){const {data,error}=await sb.from(table).select("payload").eq("origin_node",node).limit(1);if(error)throw error;return data||[];}
async function rehydrate(node:string){const {data:forward,error:fe}=await sb.from("decision_forward_ledger").select("origin_node,origin_id,created_at,symbol,horizon,target_date,model_version,prediction_hash,payload,outcome,evaluated_at,local_prediction_id,feature_fingerprint,thesis_fingerprint,confidence,uncertainty,decision_state,paper_allocation,data_cutoff,known_at_boundary,provenance_snapshot").order("created_at",{ascending:true}).limit(5000);if(fe)throw fe;return {ok:true,action:"rehydrate_authority",decision_forward_ledger:forward||[],autonomy_state:await singleton("radar_autonomy_state",node),autonomy_runs:await exact("radar_autonomy_runs",node),autonomy_experiments:await exact("radar_autonomy_experiments",node),autonomy_soak_ticks:await exact("radar_autonomy_soak_ticks",node),paper_state:await singleton("radar_paper_state",node),paper_positions:await exact("radar_paper_positions",node),paper_trades:await exact("radar_paper_trades",node),portfolio_values:await exact("radar_portfolio_values",node),experiment_memory:await exact("radar_experiment_memory",node),backfill_used:false,reconstructed:false,real_trading:false};}
Deno.serve(async(req)=>{try{const token=req.headers.get("x-radar-token")||"";if(!token||await sha(token)!==EXPECTED_HASH)return response(401,{ok:false,error:"unauthorized"});if(req.method!=="POST")return response(405,{ok:false,error:"method_not_allowed"});const body=await req.json() as Record<string,unknown>;const node=String(body.node_id||"cloud-primary");const action=String(body.action||"sync");if(action==="rehydrate_authority")return response(200,await rehydrate(node));if(action==="persist_autonomy")return response(200,await persistAutonomy(body,node));const out:Record<string,unknown>={ok:true};const models=rows(body,"model_versions",node);if(models.length){const {error}=await sb.from("model_versions").upsert(models,{onConflict:"version"});if(error)throw new Error(`model_versions: ${error.message}`);}out.model_versions=models.length;for(const table of INSERT_ONLY_TABLES){const input=rows(body,table,node);out[table]=await insertOnly(table,input);}for(const [table,key] of [["brain_lineages","lineage"],["brain_meta_methods","method"],["brain_vault_registry","vault_key"],["brain_hall_of_fame","lineage"],["brain_graveyard","lineage"]] as const){const input=rows(body,table,node);out[table]=await converge(table,input,key);}const reputation=rows(body,"source_reputation_dimensions",node).map(({origin_node:_n,origin_id:_i,...row})=>row);if(reputation.length){const {error}=await sb.from("source_reputation_dimensions").upsert(reputation,{onConflict:"source,topic,horizon"});if(error)throw error;}out.source_reputation_dimensions=reputation.length;const outcomes=rows(body,"prediction_outcomes",node);let outcomeCount=0;for(const outcome of outcomes){const {data:prediction,error:e}=await sb.from("predictions").select("id").eq("origin_node",outcome.origin_node).eq("origin_id",outcome.prediction_id).eq("horizon",outcome.horizon).maybeSingle();if(e)throw e;if(!prediction)continue;const {error}=await sb.from("prediction_outcomes").upsert({...outcome,prediction_id:prediction.id},{onConflict:"origin_node,origin_id",ignoreDuplicates:true});if(error)throw error;outcomeCount++;}out.prediction_outcomes=outcomeCount;const forward=rows(body,"decision_forward_ledger",node);let forwardOutcomes=0;if(forward.length){const inserts=forward.map(({outcome:_o,evaluated_at:_e,...row})=>row);const {error}=await sb.from("decision_forward_ledger").upsert(inserts,{onConflict:"origin_node,origin_id",ignoreDuplicates:true});if(error)throw error;for(const item of forward){if(item.outcome==null)continue;const {error:u}=await sb.from("decision_forward_ledger").update({outcome:item.outcome,evaluated_at:item.evaluated_at}).eq("origin_node",item.origin_node).eq("origin_id",item.origin_id).is("outcome",null);if(u)throw u;forwardOutcomes++;}}out.decision_forward_ledger=forward.length;out.decision_forward_outcomes=forwardOutcomes;const thesis=rows(body,"decision_thesis_events",node);out.decision_thesis_events=await insertOnly("decision_thesis_events",thesis);const shadow=rows(body,"brain_shadow_predictions",node);let shadowOutcomes=0;if(shadow.length){const inserts=shadow.map(({outcome:_o,evaluated_at:_e,...row})=>row);const {error}=await sb.from("brain_shadow_predictions").upsert(inserts,{onConflict:"origin_node,origin_id",ignoreDuplicates:true});if(error)throw error;for(const item of shadow){if(item.outcome==null)continue;const {error:u}=await sb.from("brain_shadow_predictions").update({outcome:item.outcome,evaluated_at:item.evaluated_at}).eq("origin_node",item.origin_node).eq("origin_id",item.origin_id).is("outcome",null);if(u)throw u;shadowOutcomes++;}}out.brain_shadow_predictions=shadow.length;out.brain_shadow_outcomes=shadowOutcomes;return response(200,out);}catch(error){return response(500,{ok:false,error:String((error as Error)?.message||error)});}});
