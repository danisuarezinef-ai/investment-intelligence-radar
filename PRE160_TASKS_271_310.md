# Pre-1.6 Tasks 271-310

Safety boundary: Windows stable remains **1.5.28**. No Setup build. `REAL_TRADING=false`. No automatic release, promotion, demotion or live execution.

## 271-280 — Tiered readiness and latency
271 tiered light/deep snapshots; 272 explicit readiness state machine; 273 deep-stage profiler; 274 shared deep snapshot; 275 dependency-token cache; 276 serialized prewarm scheduler; 277 CPU/I/O budget; 278 cold HTTP SLO; 279 hot p50/p95/p99 SLO; 280 latency regression gate.

## 281-290 — Durable retry authority
281 remote durable retry backend; 282 Supabase application-path proof; 283 persistent DLQ; 284 ACK-after-remote-success; 285 crash/restart test; 286 duplicate-delivery idempotence; 287 outage/recovery simulation; 288 queue saturation/backpressure; 289 poison-message isolation; 290 cross-Railway-redeploy durability proof.

## 291-300 — Provider resilience
291 wired bulkheads; 292 CLOSED/OPEN/HALF_OPEN circuit state; 293 verified provider failover; 294 adaptive 429/timeout governor; 295 timeout fail-closed behavior; 296 dual-channel health; 297 empirical dual-sync SLO; 298 remote compare after recovery; 299 partial partition/reconnect test; 300 resilience gate.

## 301-310 — Production proof v3 and master gate v4
301 evidence-derived proof only; 302 GitHub SHA + Railway SHA + protected digest; 303 tasks 151-200 in same external run; 304 tasks 201-270 in same external run; 305 measured dual-sync proof; 306 no self-certification; 307 tamper invalidation test; 308 stale-deployment rejection; 309 failed-audit propagation; 310 reproducible master gate v4.

## Evidence rules
Implementation does not imply `PASS`. Allowed task states are `PASS`, `PENDING_SAMPLE`, `PENDING_TIME`, `NOT_VERIFIED`, and `FAILED`. Task 290 requires observation across distinct Railway deployment identities. Tasks 293 and 298 remain pending until a real multi-provider/failover or recovery-reconciliation path is demonstrated. Release stays manual and fail-closed.
