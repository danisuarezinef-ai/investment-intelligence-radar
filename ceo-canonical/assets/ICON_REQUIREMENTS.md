# CEO de IAs icon gate

RC1 Setup must not be built until `assets/ceo.ico` exists in multi-resolution Windows ICO format.
The asset is a release-gate input, not a placeholder: no generic Python/browser icon is allowed in the final Setup.exe.
