# Radar Android RC track
Blocks 16-19 create the first native Android shell.
Mobile is an observer/controller; Cloud remains authoritative.
REAL_TRADING is hard-false in debug and release BuildConfig.
No API keys, broker credentials, payment capability or local financial authority are bundled.
RC1 is not Stable until CI build, signing/hash checks and physical-device evidence pass.
